.button {
    max-width: 280px;
    width: 100%;
    border-radius: 8px;
    background-color: var(--main-color);
    text-align: center;
    color: #fff;
    padding: 12px;
    font-size: 13px;
    line-height: 13px;
    border: none;
    cursor: pointer;
}
