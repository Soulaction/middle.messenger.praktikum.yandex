.link {
    display: block;
    font-size: 12px;
    line-height: 11px;
    color: var(--main-color);
    text-decoration: none;
    text-align: center;
}

.danger {
    color: var(--danger-color);
}
