.input {
    border: none;
    border-bottom: 1px solid var(--main-color);
    font-size: 13px;
    line-height: 13px;
    outline: none;
    padding: 7px 0;
    width: 100%;
}

.right-placeholder {
    text-align: end;
}
